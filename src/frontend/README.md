# Frontend / Query Layer

This directory contains code related to the user interface or data access layer.

## Files

- **`app.py`**: The main application file (likely a Streamlit or web app entry point).
- **`queries.py`**: Pre-defined SQL queries or helper functions to fetch data for the frontend.

## Usage

To run the frontend application (if applicable):

```bash
streamlit run src/frontend/app.py
```
*(Note: Verify the exact command based on the project setup)*
